import { message, Select } from "antd";
import React, { useEffect, useState } from "react";

const UnitTypeSelect = ({ ...props }) => {
  return <Select {...props} />;
};

export default UnitTypeSelect;
